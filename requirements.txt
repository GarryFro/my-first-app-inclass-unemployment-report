
# this is the "requirements.txt" file (top level)

# we list the name of each package this app requires

pandas
requests
plotly
python-dotenv

pytest # you will now need to reinstall the pip install cmd

flask